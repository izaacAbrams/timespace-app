import React, { Component } from "react";

export default class Success extends Component {
  render() {
    return (
      <div className="NotFound">
        <h2>Success!</h2>
        <p>Your appointment was created, you may now close the page.</p>
      </div>
    );
  }
}
